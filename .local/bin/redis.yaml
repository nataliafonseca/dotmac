version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: on-failure
    ports:
      - 6379:6379
    volumes:
      - redis:/data/redis
    networks:
      - redis

volumes:
  redis:
    driver: local

networks: 
  redis:
    driver: bridge